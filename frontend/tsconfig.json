{
  "compilerOptions": {
    "target": "es5",
    "module": "esnext",
    // 컴파일에 포함될 라이브러리 파일 목록입니다.
    "lib": [
      "dom",
      "dom.iterable",
      "esnext",
      "ES6", 
    ],
    "allowJs": true, // 자바스크립트의 컴파일을 허용한다.
    "noEmit": true,  // 출력을 내보내지 않는다.
    "isolatedModules": true,  // 추가 검사를 수행하여 별도의 컴파일이 안전한지 확인한다.
    "strict": true,  // 모든 엄격한 타입 검사 옵션을 활성화 한다.
    "noFallthroughCasesInSwitch": true,  // 스위치 문에 fallthrough 케이스에 대한 오류를 보고 한다.
    "moduleResolution": "node",  // 모듈 해석 방법 결정
    "allowSyntheticDefaultImports": true,  // default export가 없는 모듈에서 default imports를 허용한다. 코드 방출에는 영향을 주지 않으며, 타입 검사만 수행 한다.
    "esModuleInterop": true,  // 런타임 바벨 생태계 호환성을 위한 __importStar와 __importDefault 헬퍼를 내보내고 타입 시스템 호환성을 위해 --allowSyntheticDefaultImports를 활성화합니다.
    "skipLibCheck": true,  // 모든 선언 파일(*.d.ts)의 타입 검사를 건너뜁니다.
    "forceConsistentCasingInFileNames": true,  // 동일 파일 참조에 대해 일관성 없는 대소문자를 비활성화합니다.
    "resolveJsonModule": true,  // .json 확장자로 import된 모듈을 포함합니다.
    "jsx": "react-jsx",  // .tsx 파일에서 JSX 지원: "React", "Preserve",
    "noImplicitAny": false,  // any 타입으로 암시한 표현식과 선언에 오류를 발생 시킨다
  },
  "include": [
    "src"
  ]
}
